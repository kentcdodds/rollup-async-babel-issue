{
  "name": "rollup-async-babel-issue",
  "description":
    "Reproduction of a bug with rollup-plugin-babel/rollup and babel-plugin-external-helpers",
  "scripts": {
    "build": "npm run build:old & npm run build:new",
    "build:old": "npx rollup@0.48.2 --config --environment OUT:old",
    "build:new": "npx rollup@0.49.0 --config --environment OUT:new"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-plugin-external-helpers": "^6.22.0",
    "babel-preset-env": "^1.6.0",
    "rollup": "^0.49.2",
    "rollup-plugin-babel": "^3.0.2"
  }
}
